specVersion: "v1beta"
serviceId: "aboutme"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "aboutme-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
